import React from 'react';
import axios from '../../axios-projects';
import actionTypes from './actionTypes';


export const addTimeline = (date, username, title, content, files) => {
    axios.put('/Timeline.json',{
        
    });

}

